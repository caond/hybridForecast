% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{make_future_data}
\alias{make_future_data}
\title{Create Future Data Frame for Forecasting}
\usage{
make_future_data(hybrid_model, xreg = NA, horizon = 7)
}
\arguments{
\item{hybrid_model}{A fitted object from \code{\link{hybrid}} containing
\code{$data} with a \code{ds} column and \code{$seasonal_periods$freq}.}

\item{xreg}{Optional exogenous regressors for the forecast horizon
(\code{data.frame} or matrix). Must have \code{horizon} rows if provided.
Default \code{NA}.}

\item{horizon}{Integer, number of future periods to generate (default 7).}
}
\value{
A \code{data.frame} with:
\itemize{
  \item \code{ds} – future date/time values
  \item additional columns – if \code{xreg} is supplied
}
}
\description{
Generates a future \code{data.frame} of dates for use with a fitted
\code{hybridForecast_model}, with optional exogenous regressors.
}
\details{
The start date is taken as the maximum \code{ds} in
  \code{hybrid_model$data}, incremented according to the model's seasonal frequency.
}
\examples{
\dontrun{
fit <- hybrid(my_data)
future_df <- make_future_data(fit, horizon = 14)
}

}
